Put your real image files here (see data/data.json for exact filenames each section expects).
Mock/demo images are already here so the site looks complete out of the box. Replace any file with a real photo of the SAME filename to swap it in.
